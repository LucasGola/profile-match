/**
 * Profile Match — entrypoint.
 *
 * Placeholder do Milestone 0. A lógica real (scheduler, workers, API)
 * será montada nos milestones seguintes.
 */
function main(): void {
  console.log('Profile Match — bootstrap OK');
}

main();
